import 'package:flutter/material.dart';

class CustomFunctionFavourite extends StatelessWidget {
  const CustomFunctionFavourite({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: const Icon(Icons.favorite, size: 10),
    );
  }
}
